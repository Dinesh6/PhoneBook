<p id="notice"><%= notice %></p>
<h1>Listing Contacts of <%= @user.name %> </h1>
<table>
  <thead>
    <tr>
	<th>name</th>
      <th>Phone</th>
      <th>Note</th>
	  <th colspan="3"></th>
    </tr>
  </thead>


<% @user.phbooks.each do |contact| %>
<tr>
    <td> <%= contact.name %></td>
    <td> <%= contact.phone %></td>
    <td> <%= contact.note %></td>
    <td><%= link_to 'Show', contact %></td>
    <td><%= link_to 'Edit', edit_phbook_path(contact) %> </td>
	<td><%= link_to 'Destroy', contact, method: :delete, data: { confirm: 'Are you sure?' } %></td>
</tr>

 <% end %>
 </table>
 <br>
<%= link_to 'New Phbook', new_phbook_path %>
<%= link_to 'Back', users_path %>
